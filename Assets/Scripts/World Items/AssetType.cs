using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace OneGame {
	/// <summary>
	/// A "global" enum describing the asset type
	/// </summary>
	public enum AssetType {
		Prop = 0,
		Component = 1
	}
}